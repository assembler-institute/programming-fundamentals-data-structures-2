const exercise04 = require('../04-exercise');

describe('04-exercise', () => {
    test('use `split()` and `join()` to change the - characters', () => {
        expect(exercise04()).toBe('user_first_name');
    });
});
