const exercise08 = require('../08-exercise');

describe('08-exercise', () => {
    test('use `slice()` to get the substring `brown fox` from the sentence var', () => {
        expect(exercise08()).toBe('brown fox');
    });
});
