const exercise06 = require('../06-exercise');

describe('06-exercise', () => {
    test('use `endsWith()` to check if the string ends with `javascript`', () => {
        expect(exercise06()).toBe(true);
    });
});
