const exercise07 = require('../07-exercise');

describe('07-exercise', () => {
    test('use `includes()` to check if the string includes `brown`', () => {
        expect(exercise07()).toBe(true);
    });
});
