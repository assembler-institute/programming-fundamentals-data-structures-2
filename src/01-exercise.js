/**
 * 1. Use the native `toUpperCase()` method of strings
 *    to convert the variable `baseString` to uppercase
 * 2. Store the result in another variable
 * 3. Return the new string in uppercase
 */
function exercise01() {
    const baseString = 'hello-world';

    let conv = baseString.toUpperCase();

    return conv;
}

module.exports = exercise01;
