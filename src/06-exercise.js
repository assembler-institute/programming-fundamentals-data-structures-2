/**
 * 1. Use the native `endsWith()` method of strings
 *    to check if the variable `baseString` ends
 *    with the string `javascript`.
 *
 * 2. Store the result in a variable and return it
 */
function exercise06() {
    const baseString = 'hello-world-I-am-learning-javascript';

    // Complete to code of the function

    let endsWith = baseString.endsWith('javascript');

    return endsWith;
}

module.exports = exercise06;
