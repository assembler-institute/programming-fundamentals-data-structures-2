/**
 * Use the `Math.max()` method to find from the parameters
 * of the function the largest one of them and return it.
 *
 * The params of the function are numbers.
 */
function exercise12(a, b, c, d, e) {
    // Complete to code of the function

    let max = Math.max(a, b, c, d, e);

    return max;
}

module.exports = exercise12;
