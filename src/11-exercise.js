/**
 * Use the `Math.min()` method to find from the parameters
 * of the function the smallest one of them and return it.
 *
 * The params of the function are numbers.
 */
function exercise11(a, b, c, d, e) {
    // Complete to code of the function
}

module.exports = exercise11;
